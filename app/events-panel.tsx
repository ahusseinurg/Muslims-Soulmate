'use client';import {Localized} from './languages';import {useEffect,useState} from 'react';import {CalendarDays,MapPin,ShieldCheck} from 'lucide-react';import {Checkbox} from '@/components/ui/checkbox';
export default function EventsPanel({user,name}:{user:string,name:(id:string)=>string}){const [data,setData]=useState<any>({events:[],tickets:[],attendees:[],feeBps:500}),[error,setError]=useState(''),[busy,setBusy]=useState(false),[show,setShow]=useState(false),[title,setTitle]=useState(''),[description,setDescription]=useState(''),[venue,setVenue]=useState(''),[starts,setStarts]=useState(''),[capacity,setCapacity]=useState(20),[price,setPrice]=useState('0'),[publicVenue,setPublicVenue]=useState(false),[country,setCountry]=useState('US'),[fee,setFee]=useState('5');
async function load(){try{const r=await fetch('/api/events-management');const d:any=await r.json();if(!r.ok)throw Error(d.error);setData(d);setFee(String(d.feeBps/100));}catch(e:any){setError(e.message);}}useEffect(()=>{load()},[]);async function act(action:string,extra:any={}){setBusy(true);setError('');try{const r=await fetch('/api/events-management',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action,...extra})});const d:any=await r.json();if(!r.ok)throw Error(d.error);if(d.url){window.location.assign(d.url);return;}await load();if(action==='create'){setShow(false);setTitle('');setDescription('');}return true;}catch(e:any){setError(e.message);}finally{setBusy(false);}}
return <Localized><div className="stack"><div className="section-heading"><h2><CalendarDays/> Community events</h2><button className="primary" onClick={()=>setShow(!show)}>{show?'Close form':'Create an event'}</button></div><div className="safety-banner"><ShieldCheck size={21}/><p>Keep personal details private. Meet in public. Never send money to a new match.</p></div>{error&&<p className="error-box" role="alert">{error}</p>}{show&&<form className="panel form-grid" onSubmit={async e=>{e.preventDefault();await act('create',{title,description,venue,starts:new Date(starts).toISOString(),capacity,amount:Math.round(Number(price)*100),publicVenue});}}><label>Event name<input required maxLength={100} value={title} onChange={e=>setTitle(e.target.value)}/></label><label>Date and time (your local time)<input required type="datetime-local" value={starts} onChange={e=>setStarts(e.target.value)}/></label><label className="full">Public venue and address<input required maxLength={250} placeholder="Café, community center, park…" value={venue} onChange={e=>setVenue(e.target.value)}/></label><label className="full">Description<textarea maxLength={1500} value={description} onChange={e=>setDescription(e.target.value)}/></label><label>Maximum attendees<input type="number" min={1} max={500} required value={capacity} onChange={e=>setCapacity(Number(e.target.value))}/></label><label>Ticket price (USD; 0 = free)<input type="number" min={0} max={1000} step="0.01" required value={price} onChange={e=>setPrice(e.target.value)}/></label><p className="helper full">Free events have no app fee. Paid tickets use a {data.feeBps/100}% app fee. At ${Number(price||0).toFixed(2)}, the organizer receives ${(Math.round(Number(price||0)*100)-Math.round(Math.round(Number(price||0)*100)*data.feeBps/10000))/100} before any other provider adjustments. Payments require a verified payout account.</p><label className="agreement full"><Checkbox checked={publicVenue} onCheckedChange={v=>setPublicVenue(v===true)}/><span>I confirm this is a public venue, for adults, and I will communicate respectfully and follow the community rules.</span></label><button className="primary full" disabled={busy||!publicVenue}>Create event</button></form>}<div className="group-grid">{data.events.map((e:any)=>{const ticket=data.tickets.find((x:any)=>x.event===e.id);return <article className="panel group" key={e.id}><h3 translate="no">{e.title}</h3><p>{new Date(e.starts).toLocaleString()}</p><p><MapPin size={16}/><span translate="no">{e.venue}</span></p><p translate="no">{e.description}</p><small>Organizer: {name(e.owner)} · {e.attending}/{e.capacity} places reserved</small><h3>{e.amount?'$'+(e.amount/100).toFixed(2):'Free'}</h3><div className="actions">{ticket&&['paid','free'].includes(ticket.status)?<><strong>You’re attending</strong><button className="outline" disabled={busy} onClick={()=>act('withdraw',{event:e.id})}>{ticket.status==='paid'?'Refund information':'Cancel signup'}</button></>:<button className="primary" disabled={busy||e.starts<Date.now()} onClick={()=>act('signup',{event:e.id})}>{ticket?.status==='reserved'?'Continue checkout':e.amount?'Get ticket':'Sign up free'}</button>}{e.owner===user&&<button className="outline" disabled={busy} onClick={()=>act('cancelEvent',{event:e.id})}>Cancel event</button>}</div>{e.owner===user&&<div><h4>Attendees</h4>{data.attendees.filter((a:any)=>a.event===e.id&&['paid','free','reserved'].includes(a.status)).map((a:any)=><div className="setting-row" key={a.id}><span>{name(a.owner)} · {a.status}</span>{a.status==='paid'&&<button className="outline" disabled={busy} onClick={()=>act('refund',{event:e.id,ticket:a.id})}>Refund ticket</button>}</div>)}</div>}</article>})}</div>{!data.events.length&&<div className="empty panel"><CalendarDays/><h3>Create the first community event</h3><p>Bring people together for a respectful meeting in a public place.</p></div>}<section className="panel"><h2>Organizer payouts</h2><p>Paid-ticket proceeds go to the organizer’s connected account, less the app fee. The default app fee is 5%; payment-processing fees are separate.</p><div className="actions"><label>Country code<input maxLength={2} value={country} onChange={e=>setCountry(e.target.value.toUpperCase())}/></label><button className="primary" disabled={busy||!data.payments} onClick={()=>act('connect',{country})}>{data.payout?'Complete / update payout setup':'Connect payout account'}</button></div>{!data.payments&&<p className="helper">Paid events are not activated. The owner needs to connect Stripe first.</p>}</section>{data.reviewer&&<section className="panel"><h2>App event fee</h2><form className="actions" onSubmit={e=>{e.preventDefault();act('fee',{percent:Number(fee)})}}><label>Percentage<input type="number" min={0} max={30} step="0.1" value={fee} onChange={e=>setFee(e.target.value)}/></label><button className="primary" disabled={busy}>Save fee</button></form><p className="helper">Starts at 5%. Changes apply to new ticket checkouts; existing tickets keep their original fee.</p></section>}</div></Localized>}
